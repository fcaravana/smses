define(['app'], function (app) {

    'use strict';

    app.controller('MessageFormCtrl', [
        '$scope',
        function (scope) {
            
            console.log('Angular MessageFormCtrl');
        }
    ]);

});