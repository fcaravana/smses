define(['app'], function (app) {

    'use strict';

    app.controller('ListCtrl', [
        '$scope',
        function (scope) {
            
            console.log('Angular ListCtrl');
        }
    ]);

});